{
  "name": "songs-x402-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typegen": "wrangler types",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "dev:list": "curl http://localhost:8787/d/list && echo",
    "dev:upvote": "curl -X POST http://localhost:8787/d/1/up && echo",
    "dev:downvote": "curl -X POST http://localhost:8787/d/1/down && echo"
  },
  "dependencies": {
    "itty-router": "^5.0.22",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/node": "22.13.0",
    "@types/service-worker-mock": "^2.0.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.51.0"
  }
}
